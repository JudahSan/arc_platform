<div class="container mx-auto px-4 py-8">
  <div class="max-w-2xl mx-auto">
    <h1 class="text-3xl font-bold mb-6"><%= t('events.new.title') %></h1>
    
    <%= render 'form', event: @event %>
    
    <div class="mt-4">
      <%= link_to t('events.new.back_to_events'), events_path, class: "btn btn-ghost" %>
    </div>
  </div>
</div>
